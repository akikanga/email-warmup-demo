# Customer.io API Credentials
# Get these from: https://fly.customer.io/settings/api_credentials

CUSTOMERIO_SITE_ID=your_site_id_here
CUSTOMERIO_API_KEY=your_api_key_here

# Database Configuration (if using real database for user data)
DATABASE_URL=postgresql://user:password@localhost:5432/email_warmup

# Environment
NODE_ENV=development

# Warmup Configuration
WARMUP_TEST_MODE=true
WARMUP_TOTAL_USERS=10000
WARMUP_ENABLE_SLACK_ALERTS=false
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# ============================================================
# INSTRUCTIONS:
# ============================================================
# 1. Copy this file: cp .env.example .env
# 2. Replace placeholder values with your actual credentials
# 3. NEVER commit .env file to GitHub (it's in .gitignore)
# 4. For production, use environment variables in hosting platform
# ============================================================
